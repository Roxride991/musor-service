# 🗺️ Настройка геокодинга (преобразование адреса в координаты)

## 📋 Обзор

Реализован полноценный геокодинг с интеграцией **Яндекс.Геокодера** для преобразования адресов в координаты и проверки их соответствия зонам обслуживания.

## 🔧 Как это работает

### Алгоритм создания заказа:

```
1. Клиент создает заказ с адресом
   ↓
2. Переданы координаты (lat, lng)?
   ├─ Да → Использовать переданные координаты
   └─ Нет → GeocodingService.getCoordinates(address)
              ↓
         Яндекс.Геокодер API
              ↓
         Получение реальных координат адреса
   ↓
3. Получить активную зону обслуживания
   ↓
4. Преобразовать координаты зоны в полигон
   ↓
5. GeoUtils.isPointInPolygon(point, polygon)
   ↓
6. Точка в полигоне?
   ├─ Да → Заказ создан ✅
   └─ Нет → Ошибка "Адрес вне зоны обслуживания" ❌
```

---

## ⚙️ Настройка

### Вариант 1: С Яндекс.Геокодером (рекомендуется)

#### Шаг 1: Получите API ключ

1. Зарегистрируйтесь на https://developer.tech.yandex.ru/
2. Создайте приложение
3. Получите API ключ для Яндекс.Геокодера
4. Бесплатный тариф: **25,000 запросов/день**

#### Шаг 2: Настройте API ключ

**Через переменные окружения (рекомендуется):**
```bash
export YANDEX_GEOCODER_API_KEY=ваш_api_ключ
```

**Или в `application.yaml`:**
```yaml
geocoder:
  yandex:
    api-key: ваш_api_ключ
```

**Или через переменные окружения при запуске:**
```bash
java -jar app.jar --YANDEX_GEOCODER_API_KEY=ваш_api_ключ
```

#### Шаг 3: Проверьте настройки

В `application.yaml`:
```yaml
geocoder:
  enabled: true  # Включить геокодер
  yandex:
    api-key: ${YANDEX_GEOCODER_API_KEY:}  # Из переменной окружения
```

### Вариант 2: Без API ключа (заглушка)

Если API ключ не настроен:
- Геокодер автоматически использует заглушку
- Все адреса преобразуются в центр Оренбурга (51.7687, 55.1017)
- Работает, но точность низкая

**Для отключения геокодера:**
```yaml
geocoder:
  enabled: false  # Отключить геокодер, использовать заглушку
```

---

## 📝 Примеры использования

### Пример 1: Создание заказа с адресом (без координат)

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "comment": "Домофон не работает"
}
```

**Что происходит:**
1. Адрес "Оренбург, ул. Ленина, д. 10" передается в Яндекс.Геокодер
2. Геокодер возвращает реальные координаты этого адреса
3. Координаты проверяются на вхождение в активную зону
4. Если адрес в зоне → заказ создается ✅
5. Если адрес вне зоны → ошибка ❌

### Пример 2: Создание заказа с координатами (рекомендуется)

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "lat": 51.7750,
    "lng": 55.1100
}
```

**Что происходит:**
1. Используются переданные координаты (lat, lng)
2. Геокодер НЕ вызывается (экономия запросов)
3. Координаты проверяются на вхождение в зону
4. Если точка в зоне → заказ создается ✅

---

## 🔍 Проверка работы

### Тест 1: Проверка геокодера

**Создайте заказ с адресом:**
```json
{
    "address": "Оренбург, ул. Пушкина, д. 1",
    "pickupTime": "2025-01-20T14:00:00+03:00"
}
```

**Ожидаемое поведение:**
- Если API ключ настроен: адрес преобразуется в реальные координаты
- Если API ключ не настроен: используется центр Оренбурга (51.7687, 55.1017)
- Проверяется вхождение в зону
- Заказ создается или возвращается ошибка

### Тест 2: Проверка логирования

В логах вы увидите:
```
INFO  - Адрес 'Оренбург, ул. Пушкина, д. 1' преобразован в координаты: lat=51.7687, lng=55.1017
```

Или при ошибке:
```
WARN  - Геокодер отключен или API ключ не настроен. Используется заглушка (центр Оренбурга)
ERROR - Ошибка при геокодинге адреса '...': ...
```

---

## ⚠️ Обработка ошибок

### Ошибка: "Адрес не найден в геокодере"

**Причина:** Адрес не распознан Яндекс.Геокодером

**Решение:**
- Уточните адрес (добавьте город, регион)
- Используйте формат: "Город, улица, дом"
- Или передайте координаты напрямую

### Ошибка: "Ошибка при обращении к геокодеру"

**Причина:** Проблемы с API ключом или сетью

**Решение:**
- Проверьте API ключ
- Проверьте подключение к интернету
- Система автоматически использует заглушку (центр Оренбурга)

### Fallback механизм

Если геокодер недоступен:
1. Система автоматически использует заглушку
2. Все адреса преобразуются в центр Оренбурга
3. Заказ создается, если центр Оренбурга в зоне
4. В логах появляется предупреждение

---

## 📊 Логика работы

### С API ключом (реальный геокодер):

```
Адрес → Яндекс.Геокодер → Реальные координаты → Проверка зоны → Заказ
```

### Без API ключа (заглушка):

```
Адрес → Заглушка (центр Оренбурга) → Проверка зоны → Заказ
```

### С переданными координатами:

```
Координаты → Проверка зоны → Заказ
(геокодер не вызывается)
```

---

## 🎯 Рекомендации

### ✅ Для разработки/тестирования:

1. **Используйте координаты напрямую** - не зависит от геокодера
2. **Или получите бесплатный API ключ** - 25,000 запросов/день

### ✅ Для продакшена:

1. **Обязательно настройте API ключ** Яндекс.Геокодера
2. **Мониторьте лимиты** - 25,000 запросов/день
3. **Кэшируйте результаты** (можно добавить в будущем)
4. **Используйте координаты** когда возможно - экономия запросов

---

## 🔧 Конфигурация

### application.yaml

```yaml
geocoder:
  enabled: true  # Включить/выключить геокодер
  yandex:
    api-key: ${YANDEX_GEOCODER_API_KEY:}  # API ключ из переменной окружения
```

### Переменные окружения

```bash
# Включить/выключить геокодер
export GEOCODER_ENABLED=true

# API ключ Яндекс.Геокодера
export YANDEX_GEOCODER_API_KEY=ваш_api_ключ
```

---

## 📈 Производительность

- **С геокодером:** ~200-500ms на запрос (зависит от сети)
- **С заглушкой:** <1ms (мгновенно)
- **С координатами:** <1ms (геокодер не вызывается)

**Рекомендация:** Передавайте координаты когда возможно для максимальной производительности.

---

## 🚀 Готово к использованию!

1. ✅ Геокодер интегрирован с Яндекс.API
2. ✅ Автоматический fallback на заглушку
3. ✅ Логирование всех операций
4. ✅ Обработка ошибок
5. ✅ Проверка адреса в зоне обслуживания

**Начните использовать! Передавайте адрес, и система автоматически преобразует его в координаты и проверит зону.** 🎉








