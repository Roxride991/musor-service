# 🔧 Решение проблем при тестировании

## Проблема 1: Ошибка 400 при создании заказа

### Возможные причины и решения:

#### 1. Активная зона обслуживания не настроена

**Симптом:** Ошибка 400 с сообщением "Активная зона обслуживания не настроена"

**Решение:**
1. Создайте активную зону через эндпоинт `POST /api/zones` (требуется роль ADMIN)
2. Убедитесь, что заголовок `X-User-Id` содержит ID администратора
3. Пример запроса:

```json
POST /api/zones
Headers:
  X-User-Id: 3
  Content-Type: application/json
Body:
{
    "name": "Оренбург",
    "coordinates": [
        {"lat": 51.7687, "lng": 55.1017},
        {"lat": 51.7887, "lng": 55.1017},
        {"lat": 51.7887, "lng": 55.1217},
        {"lat": 51.7687, "lng": 55.1217}
    ]
}
```

#### 2. Адрес вне зоны обслуживания

**Симптом:** Ошибка 400 с сообщением "Адрес вне зоны обслуживания"

**Решение:**
- Убедитесь, что координаты `lat` и `lng` находятся внутри полигона зоны
- Проверьте, что координаты соответствуют адресу
- Используйте координаты из примера выше для тестирования

#### 3. Время вывоза неверное

**Симптом:** Ошибка 400 с сообщением о времени

**Решение:**
- Время вывоза должно быть минимум через 1 час от текущего времени
- Время вывоза не должно быть более 7 дней в будущем
- Используйте формат ISO 8601 с часовым поясом: `2025-01-20T14:00:00+03:00`

**Пример правильного времени:**
```json
{
    "pickupTime": "2025-01-20T14:00:00+03:00"
}
```

#### 4. Ошибка валидации данных

**Симптом:** Ошибка 400 с деталями валидации в поле `details`

**Решение:**
- Проверьте, что все обязательные поля заполнены:
  - `address` - обязателен
  - `pickupTime` - обязателен, должен быть в будущем
- Проверьте формат данных

**Пример правильного запроса:**
```json
{
    "address": "г. Оренбург, ул. Ленина, д. 1, подъезд 3",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "comment": "Домофон не работает",
    "lat": 51.7687,
    "lng": 55.1017
}
```

#### 5. Пользователь не является CLIENT

**Симптом:** Ошибка 403 Forbidden

**Решение:**
- Убедитесь, что пользователь с ID из заголовка `X-User-Id` имеет роль `CLIENT`
- Зарегистрируйте нового клиента через `POST /api/auth/register`

---

## Проблема 2: Не могу получить активную зону

### Возможные причины и решения:

#### 1. Зона не создана

**Симптом:** Ошибка 404 Not Found при `GET /api/zones/active`

**Решение:**
1. Создайте зону через `POST /api/zones` (требуется роль ADMIN)
2. Убедитесь, что заголовок `X-User-Id` содержит ID администратора
3. После создания зона автоматически становится активной

#### 2. Нет активной зоны

**Симптом:** Ошибка 404 Not Found

**Решение:**
- Проверьте, что в базе данных есть зона с `active = true`
- Создайте новую зону через API

#### 3. Неверный URL

**Симптом:** Ошибка 404 или другой код

**Решение:**
- Используйте правильный URL: `GET /api/zones/active`
- Убедитесь, что базовый URL правильный: `http://localhost:8080`

---

## 📋 Чек-лист для создания заказа

Перед созданием заказа убедитесь:

- [ ] Пользователь зарегистрирован с ролью CLIENT
- [ ] Заголовок `X-User-Id` содержит ID клиента
- [ ] Активная зона обслуживания создана (через ADMIN)
- [ ] Координаты `lat` и `lng` находятся внутри зоны
- [ ] Время вывоза минимум через 1 час
- [ ] Время вывоза не более 7 дней в будущем
- [ ] Формат времени: ISO 8601 с часовым поясом
- [ ] Все обязательные поля заполнены

---

## 🔍 Как проверить ошибку

### 1. Проверьте ответ сервера

Теперь все ошибки возвращаются в стандартизированном формате:

```json
{
    "timestamp": "2025-01-15T10:00:00+03:00",
    "status": 400,
    "error": "Bad Request",
    "message": "Описание ошибки"
}
```

### 2. Проверьте логи сервера

В логах Backend вы увидите:
- `Validation error: {...}` - ошибки валидации
- `Illegal argument: ...` - неверные аргументы
- `Illegal state: ...` - неверное состояние

### 3. Проверьте через Postman

1. Откройте консоль Postman (View → Show Postman Console)
2. Отправьте запрос
3. Проверьте ответ в консоли

---

## 🚀 Быстрое решение

### Если заказ не создается:

1. **Создайте зону обслуживания:**
   ```
   POST /api/zones
   Headers: X-User-Id: 3 (ADMIN)
   Body: {
       "name": "Оренбург",
       "coordinates": [
           {"lat": 51.7687, "lng": 55.1017},
           {"lat": 51.7887, "lng": 55.1017},
           {"lat": 51.7887, "lng": 55.1217},
           {"lat": 51.7687, "lng": 55.1217}
       ]
   }
   ```

2. **Создайте заказ:**
   ```
   POST /api/orders
   Headers: X-User-Id: 1 (CLIENT)
   Body: {
       "address": "г. Оренбург, ул. Ленина, д. 1",
       "pickupTime": "2025-01-20T14:00:00+03:00",
       "lat": 51.7687,
       "lng": 55.1017
   }
   ```

### Если зона не получается:

1. **Проверьте, что зона создана:**
   ```
   GET /api/zones/active
   ```

2. **Если 404 - создайте зону** (см. выше)

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. Проверьте логи Backend
2. Убедитесь, что база данных доступна
3. Проверьте, что все сервисы запущены
4. Проверьте формат данных в запросе

---

**После исправлений теперь вы увидите понятные сообщения об ошибках!** ✅








