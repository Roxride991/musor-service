# 🚀 Быстрый старт - Тестирование API в Postman

## Шаг 1: Импорт коллекции

1. Откройте Postman
2. Нажмите **Import** (или `Ctrl+O` / `Cmd+O`)
3. Выберите файл `postman_collection.json`
4. Коллекция появится в вашем рабочем пространстве

## Шаг 2: Настройка переменных

Откройте настройки коллекции (три точки → Edit → Variables) и проверьте:

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `baseUrl` | `http://localhost:8080` | Базовый URL API |
| `clientUserId` | `1` | ID клиента (обновится после регистрации) |
| `courierUserId` | `2` | ID курьера (обновится после регистрации) |
| `adminUserId` | `3` | ID администратора |
| `orderId` | `1` | ID заказа (обновится после создания) |
| `subscriptionId` | `1` | ID подписки (обновится после создания) |
| `otpCode` | - | OTP код (обновится после отправки) |

## Шаг 3: Быстрая последовательность тестирования

### 1. Регистрация и вход

```
1. Auth → Регистрация (CLIENT)
   → Сохраните id из ответа в clientUserId

2. Auth → Регистрация (COURIER)
   → Сохраните id из ответа в courierUserId

3. Auth → Отправить OTP код
   → Сохраните code из ответа в otpCode

4. Auth → Вход (с OTP)
   → Проверьте успешный вход
```

### 2. Профиль пользователя

```
5. Users → Получить профиль
   → Проверьте данные пользователя

6. Users → Обновить имя
   → Проверьте обновление имени
```

### 3. Зона обслуживания (важно!)

```
7. Zones → Создать/установить активную зону (ADMIN)
   → Укажите adminUserId в заголовке X-User-Id
   → Без зоны нельзя создавать заказы!
```

### 4. Подписки

```
8. Subscriptions → Создать подписку (CLIENT)
   → Сохраните id в subscriptionId

9. Subscriptions → Получить активные подписки
   → Проверьте список активных подписок
```

### 5. Заказы

```
10. Orders → Создать разовой заказ (CLIENT)
    → Сохраните id в orderId

11. Orders → Получить свободные заказы (COURIER)
    → Проверьте список свободных заказов

12. Orders → Получить статистику заказов (COURIER)
    → Проверьте счетчики

13. Orders → Принять заказ (COURIER)
    → Заказ переходит в статус ACCEPTED

14. Orders → Обновить статус заказа (COURIER)
    → Измените статус на ON_THE_WAY, затем PICKED_UP, затем COMPLETED
```

## 📋 Чек-лист основных функций

- [ ] Регистрация клиента и курьера
- [ ] Отправка и проверка OTP кода
- [ ] Получение и обновление профиля
- [ ] Создание зоны обслуживания
- [ ] Создание и управление подписками
- [ ] Создание заказов (разовых и по подписке)
- [ ] Получение свободных заказов (курьер)
- [ ] Получение активных заказов (курьер)
- [ ] Получение статистики (курьер)
- [ ] Принятие заказа курьером
- [ ] Обновление статуса заказа
- [ ] Отмена заказа клиентом

## ⚠️ Важные замечания

1. **Все запросы (кроме `/api/auth/**`) требуют заголовок `X-User-Id`**
2. **Для создания заказов нужна активная зона обслуживания**
3. **Время вывоза должно быть минимум через 1 час и не более 7 дней**
4. **Координаты адреса должны быть в зоне обслуживания**
5. **OTP код одноразовый** - после использования его нельзя использовать повторно

## 📖 Подробная документация

Для детального описания всех эндпоинтов, примеров запросов и ответов, а также обработки ошибок см. файл **`POSTMAN_TESTING_GUIDE.md`**

## 🔧 Автоматическое сохранение переменных

В Postman можно настроить автоматическое сохранение переменных через **Tests**:

```javascript
// После регистрации
if (pm.response.code === 201) {
    const response = pm.response.json();
    pm.collectionVariables.set("clientUserId", response.id);
}

// После отправки OTP
if (pm.response.code === 200 && pm.request.url.path.includes("send-code")) {
    const response = pm.response.json();
    pm.collectionVariables.set("otpCode", response.code);
}

// После создания заказа
if (pm.response.code === 201) {
    const response = pm.response.json();
    pm.collectionVariables.set("orderId", response.id);
}
```

---

**Готово! Начните тестирование с первого шага.** 🎉

