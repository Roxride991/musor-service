# ✅ Реализация полноценного геокодинга

## 🎯 Что реализовано

Реализован **полноценный геокодинг** с интеграцией **Яндекс.Геокодера** для автоматического преобразования адресов в координаты и проверки их соответствия зонам обслуживания.

---

## 🔧 Реализованные компоненты

### 1. GeocodingService (обновлен)

**Файл:** `src/main/java/com/example/core/service/GeocodingService.java`

**Функциональность:**
- ✅ Интеграция с Яндекс.Геокодер API
- ✅ Автоматический fallback на заглушку при ошибках
- ✅ Логирование всех операций
- ✅ Обработка ошибок
- ✅ Поддержка конфигурации через application.yaml

**Как работает:**
```java
public ServiceZone.Coordinate getCoordinates(String address) {
    // 1. Проверка конфигурации
    // 2. Вызов Яндекс.Геокодера (если настроен)
    // 3. Парсинг ответа и извлечение координат
    // 4. Fallback на заглушку при ошибке
}
```

### 2. GeocodingConfig (новый)

**Файл:** `src/main/java/com/example/core/config/GeocodingConfig.java`

**Функциональность:**
- ✅ Настройка RestTemplate для HTTP запросов
- ✅ Bean для использования в GeocodingService

### 3. Конфигурация application.yaml (обновлена)

**Добавлено:**
```yaml
geocoder:
  enabled: ${GEOCODER_ENABLED:true}
  yandex:
    api-key: ${YANDEX_GEOCODER_API_KEY:}
```

---

## 🔄 Полный алгоритм работы

### Создание заказа с адресом:

```
1. Клиент отправляет заказ с адресом
   {
     "address": "Оренбург, ул. Ленина, д. 10",
     "pickupTime": "2025-01-20T14:00:00+03:00"
   }
   ↓
2. OrderService.createOrder()
   ↓
3. Проверка: переданы координаты (lat, lng)?
   ├─ Да → Использовать переданные координаты
   │        (геокодер не вызывается)
   │
   └─ Нет → GeocodingService.getCoordinates(address)
            ↓
       Проверка: API ключ настроен?
       ├─ Да → Запрос к Яндекс.Геокодеру
       │        ↓
       │   Получение реальных координат адреса
       │        ↓
       │   Возврат координат
       │
       └─ Нет → Fallback на заглушку
                (центр Оренбурга: 51.7687, 55.1017)
   ↓
4. Получить активную зону обслуживания
   ↓
5. Преобразовать координаты зоны в полигон
   ↓
6. GeoUtils.isPointInPolygon(point, polygon)
   ↓
7. Точка в полигоне?
   ├─ Да → Заказ создан ✅
   └─ Нет → Ошибка "Адрес вне зоны обслуживания" ❌
```

---

## 📝 Примеры использования

### Пример 1: Заказ только с адресом (автоматический геокодинг)

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "comment": "Домофон не работает"
}
```

**Что происходит:**
1. Адрес "Оренбург, ул. Ленина, д. 10" передается в Яндекс.Геокодер
2. Геокодер возвращает реальные координаты (например: lat=51.7687, lng=55.1017)
3. Координаты проверяются на вхождение в активную зону
4. Если адрес в зоне → заказ создается ✅
5. Если адрес вне зоны → ошибка "Адрес вне зоны обслуживания" ❌

**Логи:**
```
INFO  - Адрес 'Оренбург, ул. Ленина, д. 10' преобразован в координаты: lat=51.7687, lng=55.1017
```

### Пример 2: Заказ с координатами (геокодер не вызывается)

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "lat": 51.7750,
    "lng": 55.1100
}
```

**Что происходит:**
1. Используются переданные координаты (lat, lng)
2. Геокодер НЕ вызывается (экономия запросов)
3. Координаты проверяются на вхождение в зону
4. Если точка в зоне → заказ создается ✅

---

## ⚙️ Настройка

### Вариант 1: С Яндекс.Геокодером (рекомендуется)

1. **Получите API ключ:**
   - Зарегистрируйтесь на https://developer.tech.yandex.ru/
   - Создайте приложение
   - Получите API ключ для Геокодера
   - Бесплатный тариф: 25,000 запросов/день

2. **Настройте переменную окружения:**
   ```bash
   export YANDEX_GEOCODER_API_KEY=ваш_api_ключ
   ```

3. **Или в application.yaml:**
   ```yaml
   geocoder:
     yandex:
       api-key: ваш_api_ключ
   ```

### Вариант 2: Без API ключа (заглушка)

Если API ключ не настроен:
- Система автоматически использует заглушку
- Все адреса преобразуются в центр Оренбурга (51.7687, 55.1017)
- Работает, но точность низкая

**Для отключения геокодера:**
```yaml
geocoder:
  enabled: false
```

---

## 🔍 Обработка ошибок

### Ошибка: "Адрес не найден в геокодере"

**Причина:** Адрес не распознан Яндекс.Геокодером

**Решение:**
- Уточните адрес (добавьте город, регион)
- Используйте формат: "Город, улица, дом"
- Или передайте координаты напрямую

**Fallback:** Система автоматически использует заглушку (центр Оренбурга)

### Ошибка: "Ошибка при обращении к геокодеру"

**Причина:** Проблемы с API ключом или сетью

**Решение:**
- Проверьте API ключ
- Проверьте подключение к интернету
- Система автоматически использует заглушку

### Fallback механизм

При любой ошибке геокодера:
1. ✅ Система автоматически использует заглушку
2. ✅ Все адреса преобразуются в центр Оренбурга
3. ✅ Заказ создается, если центр Оренбурга в зоне
4. ⚠️ В логах появляется предупреждение

---

## 📊 Логирование

### Успешный геокодинг:
```
INFO  - Адрес 'Оренбург, ул. Ленина, д. 10' преобразован в координаты: lat=51.7687, lng=55.1017
```

### Использование заглушки:
```
WARN  - Геокодер отключен или API ключ не настроен. Используется заглушка (центр Оренбурга)
```

### Ошибка геокодера:
```
ERROR - Ошибка при геокодинге адреса 'Оренбург, ул. Ленина, д. 10': ...
WARN  - Используется заглушка (центр Оренбурга) из-за ошибки геокодера
```

---

## ✅ Преимущества реализации

1. ✅ **Автоматическое преобразование** адресов в координаты
2. ✅ **Проверка соответствия зонам** обслуживания
3. ✅ **Fallback механизм** - работает даже без API ключа
4. ✅ **Гибкая конфигурация** - можно включить/выключить
5. ✅ **Логирование** всех операций
6. ✅ **Обработка ошибок** с понятными сообщениями
7. ✅ **Экономия запросов** - можно передавать координаты напрямую

---

## 🚀 Готово к использованию!

Теперь можно создавать заказы **просто по адресу** - система автоматически:
1. Преобразует адрес в координаты
2. Проверяет соответствие зоне обслуживания
3. Создает заказ, если адрес в зоне

**Начните использовать!** 🎉

См. также:
- `GEOCODING_SETUP.md` - детальная настройка
- `GEOCODING_QUICK_START.md` - быстрый старт








