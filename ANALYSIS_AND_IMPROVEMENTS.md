# Анализ соответствия Backend функционалу Frontend

## 📋 Обзор функционала из интерфейсов

### 1. Регистрация курьера/клиента
- **Форма:** Телефон (+7 и 10 цифр), Имя (2-50 символов, только буквы, пробелы и дефис), выбор роли (клиент/курьер)
- **Требования:** Валидация формата телефона и имени

### 2. Вход в систему
- **Форма:** Телефон и код доступа (OTP)
- **Требования:** Отправка OTP кода, проверка кода при входе

### 3. Личный кабинет клиента
- **Профиль:** Имя, телефон, роль
- **Заказы:** Список своих заказов
- **Создание заказа:** Адрес (обязательно), время забора (дата/время), комментарий, выбор подписки (если есть активная)

### 4. Личный кабинет курьера
- **Профиль:** Имя, телефон, роль
- **Статистика:** Количество свободных заказов, количество активных заказов
- **Заказы:** 
  - Отдельный список "Свободные заказы" (доступные для взятия)
  - Отдельный список "Мои активные заказы" (принятые курьером)

---

## ✅ Что уже реализовано в Backend

### Аутентификация
- ✅ Регистрация пользователя (`POST /api/auth/register`)
- ✅ Вход по телефону и коду (`POST /api/auth/login`)
- ✅ Аутентификация через заголовок `X-User-Id`

### Заказы
- ✅ Создание заказа (`POST /api/orders`)
- ✅ Получение списка заказов (`GET /api/orders`)
- ✅ Получение деталей заказа (`GET /api/orders/{id}`)
- ✅ Принятие заказа курьером (`POST /api/orders/{id}/accept`)
- ✅ Обновление статуса заказа (`PATCH /api/orders/{id}/status`)
- ✅ Отмена заказа клиентом (`DELETE /api/orders/{id}/cancel`)

### Подписки
- ✅ Создание подписки (`POST /api/subscriptions`)
- ✅ Получение списка подписок (`GET /api/subscriptions`)
- ✅ Отмена подписки (`DELETE /api/subscriptions/{id}`)
- ✅ Пауза/возобновление подписки (`PATCH /api/subscriptions/{id}/pause|resume`)

### Валидация
- ✅ Валидация формата телефона (`^\+7\d{10}$`)
- ✅ Проверка роли при регистрации
- ✅ Проверка геозоны при создании заказа
- ✅ Валидация времени забора (минимум 1 час, максимум 7 дней)

---

## ❌ Что отсутствует или требует доработки

### 1. **КРИТИЧНО: Эндпоинт для получения профиля пользователя**
**Проблема:** На фронтенде в личном кабинете отображается профиль пользователя (имя, телефон, роль), но нет API эндпоинта для его получения.

**Решение:** Создать `UserController` с эндпоинтом:
- `GET /api/users/me` - получение профиля текущего пользователя

### 2. **КРИТИЧНО: Разделение свободных и активных заказов для курьера**
**Проблема:** На фронтенде курьер видит два отдельных списка:
- "Свободные заказы" (доступные для взятия)
- "Мои активные заказы" (принятые курьером)

Но в текущей реализации `GET /api/orders` возвращает объединенный список для курьера.

**Решение:** Создать отдельные эндпоинты:
- `GET /api/orders/available` - только свободные заказы (для курьера)
- `GET /api/orders/active` - только активные заказы курьера
- Или изменить структуру ответа, добавив поле `type: "available" | "active"`

### 3. **КРИТИЧНО: Валидация имени пользователя**
**Проблема:** На фронтенде указано требование: "От 2 до 50 символов, только буквы, пробелы и дефис", но в Backend нет такой валидации.

**Текущая валидация:** Только проверка на `@NotBlank` в `RegisterRequest`.

**Решение:** Добавить валидацию:
```java
@Pattern(regexp = "^[а-яА-ЯёЁa-zA-Z\\s\\-]{2,50}$", message = "Имя должно быть от 2 до 50 символов, только буквы, пробелы и дефис")
```

### 4. **ВАЖНО: Эндпоинт для отправки OTP кода**
**Проблема:** На фронтенде есть поле "Код доступа", что подразумевает отправку OTP кода на телефон. В текущей реализации код фиксированный "0000".

**Решение:** Создать эндпоинт:
- `POST /api/auth/send-code` - отправка OTP кода на телефон
- Хранить коды в БД или кэше (Redis) с TTL 5-10 минут

### 5. **ВАЖНО: Эндпоинт для получения только активных подписок**
**Проблема:** При создании заказа на фронтенде показывается выпадающий список подписок с текстом "Если у вас есть активная подписка, выберите её". Но текущий эндпоинт `GET /api/subscriptions` возвращает все подписки.

**Решение:** Добавить query параметр или отдельный эндпоинт:
- `GET /api/subscriptions?status=ACTIVE` - только активные подписки
- Или `GET /api/subscriptions/active` - отдельный эндпоинт

### 6. **ВАЖНО: Эндпоинт для обновления имени пользователя**
**Проблема:** На фронтенде в личном кабинете имя отображается, но нет возможности его обновить (хотя `UserService.updateName` уже существует).

**Решение:** Создать эндпоинт в `UserController`:
- `PATCH /api/users/me/name` - обновление имени пользователя

### 7. **УЛУЧШЕНИЕ: Статистика для курьера**
**Проблема:** На фронтенде курьер видит счетчики:
- "Свободных заказов: 0"
- "Моих активных: 0"

Но нет эндпоинта для получения этих счетчиков.

**Решение:** Создать эндпоинт:
- `GET /api/orders/stats` - возвращает объект с количеством свободных и активных заказов

### 8. **УЛУЧШЕНИЕ: Структура ответа для заказов курьера**
**Проблема:** При получении списка заказов курьером, фронтенд не может различить, какие заказы свободные, а какие активные.

**Решение:** Добавить в `OrderResponse` поле:
```java
private Boolean isAvailable; // true для свободных, false для активных
```

---

## 🔧 Рекомендации по улучшению Backend

### 1. **Создать UserController**
```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    // GET /api/users/me - профиль
    // PATCH /api/users/me/name - обновление имени
}
```

### 2. **Разделить эндпоинты заказов для курьера**
```java
// В OrderController добавить:
@GetMapping("/available")  // только для курьера
@GetMapping("/active")     // только для курьера
```

### 3. **Добавить OTP сервис**
```java
@Service
public class OtpService {
    // Генерация и отправка OTP
    // Проверка OTP при входе
}
```

### 4. **Улучшить валидацию**
- Добавить валидацию имени по паттерну
- Добавить валидацию формата даты/времени
- Улучшить сообщения об ошибках

### 5. **Добавить обработку ошибок**
- Создать глобальный `@ControllerAdvice` для обработки исключений
- Стандартизировать формат ответов об ошибках

### 6. **Добавить логирование**
- Логировать важные операции (создание заказа, принятие заказа, вход)
- Логировать ошибки

---

## 🎨 Рекомендации по улучшению Frontend

### 1. **Обработка ошибок**
- Показывать понятные сообщения об ошибках пользователю
- Обрабатывать 401 ошибки (истекшая сессия) с предложением перелогиниться

### 2. **Валидация на клиенте**
- Валидация формата телефона перед отправкой
- Валидация имени по паттерну
- Валидация даты/времени

### 3. **UX улучшения**
- Показывать загрузку при создании заказа
- Показывать успешные сообщения после операций
- Автоматическое обновление списка заказов после принятия

### 4. **Обработка сессий**
- Автоматическая проверка сессии при загрузке страницы
- Редирект на страницу входа при 401 ошибке

---

## 📊 Приоритеты внедрения

### Высокий приоритет (критично для работы)
1. ✅ Создать `UserController` с эндпоинтом `/api/users/me`
2. ✅ Разделить эндпоинты заказов для курьера (`/available` и `/active`)
3. ✅ Добавить валидацию имени пользователя

### Средний приоритет (важно для UX)
4. ✅ Создать эндпоинт для отправки OTP кода
5. ✅ Добавить эндпоинт для получения активных подписок
6. ✅ Добавить эндпоинт для обновления имени

### Низкий приоритет (улучшения)
7. ✅ Добавить статистику для курьера
8. ✅ Улучшить структуру ответов
9. ✅ Добавить глобальную обработку ошибок

---

## 📝 Итоговая оценка

**Соответствие Backend функционалу Frontend: ~70%**

**Основные проблемы:**
- Отсутствует эндпоинт для получения профиля
- Заказы для курьера не разделены на свободные/активные
- Нет валидации имени по требованиям
- OTP код фиксированный (MVP)

**Сильные стороны:**
- Хорошая структура проекта
- Правильная валидация телефона
- Корректная работа с ролями
- Хорошая логика работы с заказами и подписками

