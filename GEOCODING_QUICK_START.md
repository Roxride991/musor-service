# 🚀 Быстрый старт: Геокодинг адресов

## ✅ Что реализовано

Теперь система **автоматически преобразует адрес в координаты** и проверяет их соответствие зонам обслуживания!

### Как это работает:

1. **Вы передаете адрес** в заказе
2. **Система преобразует адрес** в координаты через Яндекс.Геокодер
3. **Проверяется**, находятся ли координаты в активной зоне
4. **Если да** → заказ создается ✅
5. **Если нет** → ошибка "Адрес вне зоны обслуживания" ❌

---

## 🎯 Два способа создания заказа

### Способ 1: Только адрес (новое!) 🆕

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "comment": "Домофон не работает"
}
```

**Что происходит:**
- Адрес автоматически преобразуется в координаты
- Координаты проверяются на вхождение в зону
- Заказ создается, если адрес в зоне

### Способ 2: Адрес + координаты (рекомендуется)

**Запрос:**
```json
POST /api/orders
Headers:
  X-User-Id: 1
  Content-Type: application/json
Body:
{
    "address": "Оренбург, ул. Ленина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "lat": 51.7750,
    "lng": 55.1100
}
```

**Преимущества:**
- Геокодер не вызывается (экономия запросов)
- Максимальная точность
- Быстрее работает

---

## ⚙️ Настройка (опционально)

### Если хотите использовать реальный геокодер:

1. **Получите API ключ** на https://developer.tech.yandex.ru/
2. **Настройте переменную окружения:**
   ```bash
   export YANDEX_GEOCODER_API_KEY=ваш_api_ключ
   ```

### Если API ключ не настроен:

- Система автоматически использует заглушку
- Все адреса преобразуются в центр Оренбурга
- Работает, но точность ниже

---

## 📝 Примеры в Postman

### Пример 1: Заказ только с адресом

```
POST /api/orders
Headers:
  X-User-Id: 1
Body:
{
    "address": "Оренбург, ул. Пушкина, д. 10, подъезд 3",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "comment": "Домофон не работает"
}
```

**Результат:**
- Адрес преобразуется в координаты
- Проверяется вхождение в зону
- Заказ создается или возвращается ошибка

### Пример 2: Заказ с адресом и координатами

```
POST /api/orders
Headers:
  X-User-Id: 1
Body:
{
    "address": "Оренбург, ул. Пушкина, д. 10",
    "pickupTime": "2025-01-20T14:00:00+03:00",
    "lat": 51.7750,
    "lng": 55.1100
}
```

**Результат:**
- Используются переданные координаты
- Геокодер не вызывается
- Быстрая проверка зоны

---

## ⚠️ Важно

1. **Адрес должен быть в активной зоне** - иначе ошибка
2. **Формат адреса:** "Город, улица, дом" (лучше распознается)
3. **Без API ключа** используется заглушка (центр Оренбурга)
4. **С API ключом** используются реальные координаты адреса

---

## 🔍 Проверка работы

1. **Создайте зону обслуживания** (если еще не создана)
2. **Создайте заказ с адресом** (без координат)
3. **Проверьте логи** - должны быть сообщения о геокодинге
4. **Заказ должен создаться**, если адрес в зоне

---

**Готово! Теперь можно создавать заказы просто по адресу!** 🎉

См. также: `GEOCODING_SETUP.md` для детальной настройки








